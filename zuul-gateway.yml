spring:
  session:
    store-type: redis
    timeout: 3600
  redis:
    port: 12000
zuul: 
  retryable: true
  routes:
    order-management-ms:
      path: /order-management/**
      sensitiveHeaders: Authorization, Set-Cookie
security:
  user:
    name: gateway
    password: password

