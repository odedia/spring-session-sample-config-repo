spring:
  session:
    store-type: redis
zuul: 
 retryable: true
 routes:
   order-management-ms: /order-management/**
security:
  user:
    name: gateway
    password: password
    sessions: ALWAYS

